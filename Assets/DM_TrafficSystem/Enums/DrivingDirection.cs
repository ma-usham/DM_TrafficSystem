namespace Darkmatter.TrafficSystem
{
    /// <summary>
    /// Defines which side of the road is treated as the forward travel direction.
    /// </summary>
    public enum DrivingDirection
    {
        Left,
        Right,
    }
}
